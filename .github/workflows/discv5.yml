# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches:
      - "*"
    paths:
      - "Lantern.Discv5.Enr/**"
      - "Lantern.Discv5.Rlp/**"
      - "Lantern.Discv5.Tests/**"
      - "Lantern.Discv5.Topic/**"
  pull_request:
    branches: 
      - "*"
    paths:
      - "Lantern.Discv5.Enr/**"
      - "Lantern.Discv5.Rlp/**"
      - "Lantern.Discv5.Tests/**"
      - "Lantern.Discv5.Topic/**"

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      id: test
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    
    - name: Get changed files
      id: file-changes
      #uses: trilom/file-changes-action@v1.2.4
      run: |
        echo "Token is ${{ secrets.SONAR_TOKEN }}"
      # with:
      #   githubRepo: trilom/file-changes-action
      #   githubToken: ${{ secrets.GITHUB_TOKEN }}
      #run: echo "cat $HOME/files.json" >> "$GITHUB_OUTPUT"
        # cat $HOME/files.json
        # cat $HOME/files_modified.json
        # cat $HOME/files_added.json
        # cat $HOME/files_removed.json
        # echo '${{ steps.file-changes.outputs.files}}'
        # echo '${{ steps.file_changes.outputs.files_modified}}'
        # echo '${{ steps.file_changes.outputs.files_added}}'
        # echo '${{ steps.file_changes.outputs.files_removed}}'

    # - name: Restore Project_Enr if changed
    #   if: contains(steps.file-changes.outputs.files_modified, 'Project_1') || contains(steps.file-changes.outputs.files_added, 'Project_1')
    #   run: dotnet restore Project_1/Project_1.csproj
      
    # - name: Build Project_Rlp if changed
    #   if: contains(steps.file-changes.outputs.files_modified, 'Project_1') || contains(steps.file-changes.outputs.files_added, 'Project_1')
    #   run: dotnet build Project_1/Project_1.csproj --configuration Release --no-restore
  sonar:
    name: Build and analyze SonarCloud
    runs-on: windows-latest
    steps:
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: 11
          distribution: 'zulu' # Alternative distribution options are available.
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Cache SonarCloud packages
        uses: actions/cache@v3
        with:
          path: ~\sonar\cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache SonarCloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@v3
        with:
          path: .\.sonar\scanner
          key: ${{ runner.os }}-sonar-scanner
          restore-keys: ${{ runner.os }}-sonar-scanner
      - name: Install SonarCloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          New-Item -Path .\.sonar\scanner -ItemType Directory
          dotnet tool update dotnet-sonarscanner --tool-path .\.sonar\scanner
      - name: Build and analyze
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        shell: powershell
        run: |
          .\.sonar\scanner\dotnet-sonarscanner begin /k:"Pier-Two_Lantern.Discv5" /o:"pier-two" /d:sonar.login="${{ secrets.SONAR_TOKEN }}" /d:sonar.host.url="https://sonarcloud.io"
          dotnet build
          .\.sonar\scanner\dotnet-sonarscanner end /d:sonar.login="${{ secrets.SONAR_TOKEN }}"
